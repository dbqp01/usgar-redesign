---
import Layout from '../layouts/Layout.astro';
import MapSection from '../components/MapSection.astro';
import { useTranslations } from '../i18n/utils';
import { siteSettings } from '../data/settings';

const lang = (Astro.currentLocale || 'en') as 'en' | 'es';
const t = useTranslations(lang);
---

<Layout title={`${t('contact.title')} — USGAR Hotels`} lang={lang}>
  <!-- Header Banner -->
  <section class="relative pt-32 pb-16 px-4 bg-gradient-to-b from-primary/10 via-transparent to-transparent overflow-hidden">
    <div class="max-w-4xl mx-auto text-center relative z-10">
      <span class="text-sm font-semibold text-primary dark:text-purple-pastel uppercase tracking-widest">{t('nav.contact')}</span>
      <h1 class="font-display text-4xl md:text-6xl mt-2 mb-6 leading-tight text-text-primary-light dark:text-text-primary-dark">
        {t('contact.title')}
      </h1>
      <p class="text-lg md:text-xl text-text-secondary-light dark:text-text-secondary-dark max-w-2xl mx-auto font-body font-light leading-relaxed">
        {t('contact.subtitle')}
      </p>
    </div>
  </section>

  <!-- Contact Grid -->
  <section class="pb-24 px-4 max-w-7xl mx-auto">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 md:gap-12 items-start">
      
      <!-- Left Column: Support Form (5 cols) -->
      <div class="lg:col-span-5 bg-surface-card-light dark:bg-surface-card-dark p-6 md:p-8 rounded-3xl border border-black/5 dark:border-white/5 shadow-sm space-y-6">
        <h2 class="font-display text-2xl text-primary dark:text-secondary font-bold border-b border-black/5 dark:border-white/5 pb-3">
          {t('contact.getInTouch')}
        </h2>

        <form id="contact-form" class="space-y-4">
          <!-- Name -->
          <div>
            <label for="contact-name" class="block text-xs font-semibold uppercase tracking-wider text-text-secondary-light dark:text-text-secondary-dark mb-2">
              {t('contact.formName')} *
            </label>
            <input
              type="text"
              id="contact-name"
              name="name"
              required
              placeholder="e.g. John Doe"
              class="w-full px-4 py-3 rounded-xl bg-stone-50 dark:bg-surface-dark border border-black/10 dark:border-white/10 text-sm text-text-primary-light dark:text-text-primary-dark focus:ring-2 focus:ring-primary focus:border-transparent outline-none transition-all placeholder:text-stone-400"
            />
          </div>

          <!-- Email -->
          <div>
            <label for="contact-email" class="block text-xs font-semibold uppercase tracking-wider text-text-secondary-light dark:text-text-secondary-dark mb-2">
              {t('contact.formEmail')} *
            </label>
            <input
              type="email"
              id="contact-email"
              name="email"
              required
              placeholder="e.g. john@example.com"
              class="w-full px-4 py-3 rounded-xl bg-stone-50 dark:bg-surface-dark border border-black/10 dark:border-white/10 text-sm text-text-primary-light dark:text-text-primary-dark focus:ring-2 focus:ring-primary focus:border-transparent outline-none transition-all placeholder:text-stone-400"
            />
          </div>

          <!-- Message -->
          <div>
            <label for="contact-message" class="block text-xs font-semibold uppercase tracking-wider text-text-secondary-light dark:text-text-secondary-dark mb-2">
              {t('contact.formMessage')} *
            </label>
            <textarea
              id="contact-message"
              name="message"
              required
              rows="4"
              placeholder={t('contact.messagePlaceholder')}
              class="w-full px-4 py-3 rounded-xl bg-stone-50 dark:bg-surface-dark border border-black/10 dark:border-white/10 text-sm text-text-primary-light dark:text-text-primary-dark focus:ring-2 focus:ring-primary focus:border-transparent outline-none transition-all placeholder:text-stone-400 resize-none"
            ></textarea>
          </div>

          <!-- Submit Button -->
          <button
            type="submit"
            id="btn-contact-submit"
            class="w-full bg-primary hover:bg-primary-dark text-white font-semibold py-3.5 rounded-xl transition-all duration-300 hover:shadow-lg hover:shadow-primary/25 hover:-translate-y-0.5 text-sm uppercase tracking-wider cursor-pointer flex items-center justify-center gap-2"
          >
            <span>{t('contact.formSubmit')}</span>
            <!-- Spinner -->
            <svg id="contact-spinner" class="hidden animate-spin h-5 w-5 text-white" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
          </button>

          <!-- Success message -->
          <div id="contact-success" class="hidden p-4 rounded-xl bg-[#2DB5A0]/10 border border-[#2DB5A0]/20 text-[#1F9484] dark:text-[#4ECBB8] text-xs leading-relaxed flex items-start gap-2">
            <svg class="w-4 h-4 text-[#2DB5A0] flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
            <span>{t('contact.successMessage')}</span>
          </div>
        </form>
      </div>

      <!-- Right Column: Tourist Channels & Info (7 cols) -->
      <div class="lg:col-span-7 space-y-8">
        
        <!-- Channels Section -->
        <div class="bg-surface-card-light dark:bg-surface-card-dark p-6 md:p-8 rounded-3xl border border-black/5 dark:border-white/5 shadow-sm space-y-6">
          <h2 class="font-display text-2xl text-primary font-bold border-b border-black/5 dark:border-white/5 pb-3">
            {t('contact.directChannels')}
          </h2>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <!-- WhatsApp -->
            <a
              href={`https://wa.me/${siteSettings.whatsappNumber}?text=${encodeURIComponent(t('contact.whatsappQuery'))}`}
              target="_blank"
              rel="noopener noreferrer"
              class="flex items-center gap-4 p-4 rounded-2xl bg-stone-50 dark:bg-stone-900/40 border border-black/5 dark:border-white/5 hover:border-[#2DB5A0]/30 hover:bg-[#2DB5A0]/5 transition-all duration-300 group"
            >
              <div class="w-10 h-10 rounded-xl bg-[#2DB5A0]/10 text-[#2DB5A0] flex items-center justify-center flex-shrink-0 group-hover:scale-105 transition-transform">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
                </svg>
              </div>
              <div>
                <h4 class="font-semibold text-xs text-text-primary-light dark:text-text-primary-dark">{t('contact.whatsappText')}</h4>
                <p class="text-[11px] text-text-secondary-dark mt-0.5">{siteSettings.phone}</p>
              </div>
            </a>

            <!-- Email -->
            <a
              href={`mailto:${siteSettings.email}`}
              class="flex items-center gap-4 p-4 rounded-2xl bg-stone-50 dark:bg-stone-900/40 border border-black/5 dark:border-white/5 hover:border-primary/30 hover:bg-primary/5 transition-all duration-300 group"
            >
              <div class="w-10 h-10 rounded-xl bg-primary/10 text-primary flex items-center justify-center flex-shrink-0 group-hover:scale-105 transition-transform">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
              </div>
              <div>
                <h4 class="font-semibold text-xs text-text-primary-light dark:text-text-primary-dark">{t('contact.emailText')}</h4>
                <p class="text-[11px] text-text-secondary-dark mt-0.5">{siteSettings.email}</p>
              </div>
            </a>

            <!-- International Call -->
            <a
              href={`tel:+${siteSettings.phoneRaw}`}
              class="flex items-center gap-4 p-4 rounded-2xl bg-stone-50 dark:bg-stone-900/40 border border-black/5 dark:border-white/5 hover:border-secondary/30 hover:bg-secondary/5 transition-all duration-300 group"
            >
              <div class="w-10 h-10 rounded-xl bg-secondary/10 text-secondary flex items-center justify-center flex-shrink-0 group-hover:scale-105 transition-transform">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.94.725l.548 2.2a1 1 0 01-.321.988l-1.305.98a10.582 10.582 0 004.872 4.872l.98-1.305a1 1 0 01.988-.321l2.2.548a1 1 0 01.725.94V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                </svg>
              </div>
              <div>
                <h4 class="font-semibold text-xs text-text-primary-light dark:text-text-primary-dark">{t('contact.callText')}</h4>
                <p class="text-[11px] text-text-secondary-dark mt-0.5">{t('contact.internationalCall')} {siteSettings.phone}</p>
              </div>
            </a>

            <!-- OTAs & Portals -->
            <div class="flex items-center gap-3 p-4 rounded-2xl bg-stone-50 dark:bg-stone-900/40 border border-black/5 dark:border-white/5">
              <span class="text-text-secondary-dark flex-shrink-0">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
                </svg>
              </span>
              <div class="flex gap-2">
                <a
                  href="https://tripadvisor.com"
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-[10px] bg-stone-200 dark:bg-stone-800 hover:bg-stone-300 dark:hover:bg-stone-700 px-3 py-1.5 rounded-lg font-semibold transition-colors"
                >
                  TripAdvisor
                </a>
                <a
                  href="https://booking.com"
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-[10px] bg-stone-200 dark:bg-stone-800 hover:bg-stone-300 dark:hover:bg-stone-700 px-3 py-1.5 rounded-lg font-semibold transition-colors"
                >
                  Booking.com
                </a>
              </div>
            </div>

          </div>
        </div>

        <!-- Travel Guide & Airport Pickup Info -->
        <div class="bg-surface-card-light dark:bg-surface-card-dark p-6 md:p-8 rounded-3xl border border-black/5 dark:border-white/5 shadow-sm space-y-6">
          <h2 class="font-display text-2xl text-primary font-bold border-b border-black/5 dark:border-white/5 pb-3">
            {t('contact.directionsTitle')}
          </h2>
          <p class="text-sm text-text-secondary-light dark:text-text-secondary-dark leading-relaxed">
            {t('contact.directionsDesc')}
          </p>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 pt-2">
            <!-- Airport Pickup -->
            <div class="p-5 rounded-2xl bg-[#9B6CB5]/5 border border-[#9B6CB5]/10 space-y-2">
              <h4 class="font-semibold text-sm text-[#7A4F96] dark:text-[#B48ACC] flex items-center gap-2">
                <svg class="w-4 h-4 text-[#9B6CB5] flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M8 17a2 2 0 100-4 2 2 0 000 4zm8 0a2 2 0 100-4 2 2 0 000 4zm2-10h-3V4H5v10h13V7z" />
                </svg>
                {t('contact.airportPickup')}
              </h4>
              <p class="text-xs text-text-secondary-light dark:text-text-secondary-dark leading-relaxed">
                {t('contact.airportPickupDesc')}
              </p>
            </div>

            <!-- Altitude Safety Warning -->
            <div class="p-5 rounded-2xl bg-[#F5B731]/5 border border-[#F5B731]/10 space-y-2">
              <h4 class="font-semibold text-sm text-[#D99E1B] dark:text-[#F7C75A] flex items-center gap-2">
                <svg class="w-4 h-4 text-[#F5B731] flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 15a4.5 4.5 0 004.5 4.5H18a3.75 3.75 0 001.332-7.257 3 3 0 00-3.758-3.848 5.25 5.25 0 00-10.233 2.33A4.502 4.502 0 002.25 15z" />
                </svg>
                {t('contact.altitudeTitle')}
              </h4>
              <p class="text-xs text-text-secondary-light dark:text-text-secondary-dark leading-relaxed">
                {t('contact.altitudeDesc')}
              </p>
            </div>
          </div>
        </div>

      </div>

    </div>
  </section>

  <!-- Interactive Map Section -->
  <MapSection />
</Layout>

<script>
  let abortController = new AbortController();

  function initContactForm() {
    // Abort previous listeners to avoid duplicates
    abortController.abort();
    abortController = new AbortController();
    const signal = abortController.signal;

    const form = document.getElementById('contact-form') as HTMLFormElement;
    const btnSubmit = document.getElementById('btn-contact-submit') as HTMLButtonElement;
    const spinner = document.getElementById('contact-spinner');
    const successBanner = document.getElementById('contact-success');

    if (!form || !btnSubmit || !spinner || !successBanner) return;

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      // Hide success banner
      successBanner.classList.add('hidden');
      
      // Disable button & show spinner
      btnSubmit.disabled = true;
      spinner.classList.remove('hidden');

      // Simulate network request
      await new Promise(resolve => setTimeout(resolve, 1500));

      // Reset state and show success banner
      spinner.classList.add('hidden');
      btnSubmit.disabled = false;
      successBanner.classList.remove('hidden');
      form.reset();
    }, { signal });
  }

  document.addEventListener('astro:page-load', initContactForm);
</script>
