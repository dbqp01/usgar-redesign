---
// src/components/profile/ProfileHeader.astro
// Quiet Luxury header card: avatar/initial ring, name/email, logout action.
// The photo/name/email are populated by the page's inline profile script.
import { useTranslations, type Locale } from '../../i18n/utils';

const lang = (Astro.currentLocale || "en") as Locale;
const t = useTranslations(lang);
---

<!-- Profile Card (Quiet Luxury Header) -->
<div id="profile-card" class="bg-surface-card-light dark:bg-surface-card-dark p-8 md:p-10 rounded-3xl border border-black/10 dark:border-white/10 shadow-xl flex flex-col md:flex-row items-start md:items-center justify-between gap-6 relative overflow-hidden">
  <!-- Background Ambient Glow -->
  <div class="absolute -right-20 -top-20 w-64 h-64 bg-primary/10 rounded-full blur-3xl pointer-events-none"></div>

  <div class="flex items-center gap-6 relative z-10">
    <!-- User Avatar / Initial Ring -->
    <div class="relative w-20 h-20 rounded-full overflow-hidden bg-primary/15 border-2 border-gold-bright shadow-lg flex-shrink-0 flex items-center justify-center">
      <img id="profile-user-photo" src="" alt="User Profile" class="w-full h-full object-cover hidden" />
      <span id="profile-user-initial" class="text-3xl font-display text-primary dark:text-gold-bright">U</span>
    </div>

    <div class="space-y-1">
      <div class="flex items-center gap-3 flex-wrap">
        <h1 id="profile-user-name" class="font-display text-2xl md:text-3xl tracking-tight">
          Loading Profile...
        </h1>
        <span id="profile-provider-tag" class="text-[10px] uppercase font-mono tracking-widest px-2.5 py-0.5 rounded-full bg-primary/10 text-primary border border-primary/20 font-semibold">
          Guest
        </span>
      </div>
      <p id="profile-user-email" class="text-text-secondary-light dark:text-text-secondary-dark text-sm font-mono">
        -
      </p>
    </div>
  </div>

  <!-- Action: Logout -->
  <div class="relative z-10 flex items-center gap-3 self-end md:self-center">
    <button
      id="profile-logout-btn"
      type="button"
      class="px-6 py-2.5 rounded-full border border-black/10 dark:border-white/10 hover:border-rose-500 hover:text-rose-500 text-xs font-semibold uppercase tracking-wider transition-[border-color,color] duration-300 inline-flex items-center gap-2 cursor-pointer"
    >
      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
      </svg>
      <span>{t('auth.signOut')}</span>
    </button>
  </div>
</div>
